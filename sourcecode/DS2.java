// STRATEGY PATTERn
// constructor makes sure we assign ints to DS2
public class DS2 extends DS<Integer>{
    public DS2(Integer temp_p, Integer temp_v, Integer price, Integer cf) {
        // Call the constructor of the superclass (DS<Integer>)
        super(temp_p, temp_v, price, cf);
    }
}
